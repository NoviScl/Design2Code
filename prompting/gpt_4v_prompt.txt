Write the HTML code to fully reproduce the webpage in the image, including the text, fonts, layout, and colors.
Embed all CSS styles in the same HTML code. For all images, just use "rick.jpg" as the placeholder file source. 